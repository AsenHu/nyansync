# 协议规范 版本 1

TCP 连接由 sender 发起和关闭，receiver 只负责处理连接。

协议的第一个字节是命令字节，后面跟着数据。其中，0 - 127 是命令，128 - 255 是响应。

所有命令都由 sender 发送，receiver 遇到错误时，不应该退出程序，以避免拒绝服务攻击。

通常情况下，sender 的数量不会多于一个，因此哪些目录已上传等信息由 sender 管理。因此当有多个 sender 上传文件时，将会出现竞态条件，这可能会浪费流量。例如，sender A 和 sender B 同时列出了目录 0x1234，然后由于此时双方都尚未上传任何文件，这时如果 A 和 B 同时有文件，但该文件 receiver 没有，A 和 B 就会分别上传该文件，这将导致竞态条件。这件事比较难发生，receiver 抛出 warn 即可。

## 检查版本

检查版本非强制行为，sender 应该在每次建立连接时检查版本，版本号不一致时应该考虑 Panic，因为 sender 没有更多地址可以尝试。

receiver 接收到版本不一致的连接后，应当丢弃所有接收到的数据。receiver 应该主动关闭连接。

### 请求

| 1 字节 | 1 字节  |
|-------|---------|
| 命令   | 协议版本 |

其中：

- 命令：始终为 0
- 协议版本：目前为 0

### 响应

| 1 字节 | 1 字节  |
|-------|---------|
| 响应   | 协议版本 |

其中：

- 响应：始终为 128
- 协议版本：目前为 0

## 列出目录

sender 获取指定目录下所有文件的哈希值。

返回的列表仅保证列出时已经存在的文件，receiver 可能在列出后删除或添加文件。

### 请求

| 1 字节 | 2 字节  |
|-------|---------|
| 命令   | 目录名称 |

其中：
- 命令：始终为 1
- 目录名称：目录的名称，使用 2 字节表示，范围为 0x0000 - 0xffff。每个目录对应一个唯一的值。

例如：0x0000 代表 cache/00/00，0xabcd 代表 cache/ab/cd。

### 响应

**整体结构**

响应由一个响应码，后跟一个或多个数据分片组成。

`[响应码: 129] [数据分片 1] [数据分片 2] ... [最后一个数据分片]`

**数据分片结构**

| uint16 | 哈希数量 * 20 字节 |
|--------|------------------|
| 哈希数量 | 文件哈希值        |

**核心逻辑：如何判断结束**

客户端通过循环读取每个分片头部的 u16 哈希数量来判断：

- 如果数量等于 65535 (u16::MAX)：说明这是一个满载的分片，后面还有更多分片，需要继续接收。
- 如果数量小于 65535（从 0 到 65534）：说明这是最后一个分片，接收完这批数据后，整个响应结束。

## 发送文件

### 请求

| 1 字节 | uint32  | 20 字节  | uint16  | uint16  | 1 字节  |
|-------|---------|----------|---------|---------|--------|
| 命令   | 文件大小 | 文件哈希值 | 图片宽度 | 图片高度 | 文件类型 |

其中：
- 命令：始终为 2
- 文件大小：uint32 类型，表示文件的大小
- 文件哈希值：20 字节，使用 SHA-1 哈希算法计算的文件哈希值
- 图片宽度：uint16 类型，表示图片的宽度
- 图片高度：uint16 类型，表示图片的高度
- 文件类型：1 字节，表示文件的类型，0x0 代表 GIF，0x1 代表 JPG，0x2 代表 WEBP，0x3 代表 PNG。

receiver 在接收到请求后，应该做出以下检查。

1. 文件类型应该在 0x0 - 0x3 范围内。
2. 检查图片的宽度和高度，应该介于 100 和 20000 之间（包含）。
3. 图片的宽 + 高大于（不包含）250。
4. 文件的大小应该大于 0，对于不同的格式，大小的限制如下：
   - GIF：小于 10MiB
   - JPG：小于 20MiB
   - WEBP：小于 20MiB
   - PNG：小于 50MiB
5. 文件哈希值应该与接收到的文件内容一致。

如果检查不通过，receiver 应该关闭连接并丢弃从该命令起往后的所有数据（即保留已经接收到的图片，但拒绝后续命令）。

没有响应